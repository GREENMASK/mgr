<%=content_for :title,"Profile"%>

<div id="profile">
  <table>
    <spam>Użytkownik:</spam>
    <tr>
      <th><th>
      <th><th>
    </tr>
    <tr>
      <td>Id:</td>
      <td><%= @user.id %></td>
    </tr>
    <tr>
    <td>
     Email:
    </td>
     <td>
     <%= @user.email %>
    </td>
    </tr>
    <tr>
      <td>Utworzono:</td>
      <td><%= @user.created_at.to_date %></td>
    </tr>
    <tr>
      <td>Edytowano:</td>
      <td><%= @user.updated_at.to_date %></td>
    </tr>
    <tr>
      <td>Liczba plików gpx:</td>
      <td><%= @location_name.count %></td>
   </tr>
  <table>
  </br>
  <%= link_to "cos", edit_user_registration_path(:id => @user.id) %>
  <table>
    <spam>Pliki gpx użytkownika:</spam>
    <tr>
      <th></th>
      <th></th>
    </tr>
    <% @location_name.each do |file|%>
      <tr>
        <td><%= file %> </td>
        <td> <%= link_to "Usuń", location_path(file), :method => :delete,:confirm => true %></td>
        <td><%= link_to "Pobierz",download_gpx_locations_path(file)%></td>
      </tr>
    <% end %>
    
  </table>

</div>
